{% extends 'base.html.twig' %}
{% block css %}
    <style>
        #detalle_fechareg_date_month{
            display:none;
        }
    </style>
{% endblock%}
{% block body %}
    <h1>detalle creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('detalle_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
